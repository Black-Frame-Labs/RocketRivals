# Downloads

Put the signed release APK here and name it:

`rocket-rivals-latest.apk`

The homepage download button points to:

`downloads/rocket-rivals-latest.apk`

For GitHub Pages, commit the APK if the file size is acceptable, or change the download link in `index.html` to a GitHub Releases URL.
